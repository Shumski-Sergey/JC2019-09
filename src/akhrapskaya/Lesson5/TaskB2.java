package akhrapskaya.Lesson5;

//import java.util.Scanner;

//2. Выбрать последовательность неповторяющихся символов в алфавитнои порядке.Пробелы нужно игнорировать

public class TaskB2 {
   /* public static void  main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Введите строку:");
        String s = sc.nextLine();

        System.out.println(s);
        if(s.matches("^(i?)[a-zа-я]+$")) {


            boolean n = true;
            for (int j = 1; j < s.length(); j++) {
                if (s.charAt(j) <= s.charAt(j - 1)) {
                    n = false;
                    break;
                }
            }
            if (n) {
                System.out.println("Буквы в строке идут в алфавитном порядке.");

            }
        }
        else
            System.out.println("Некорректный ввод в строке должны быть только буквы и пробелы.");
    }*/
}
